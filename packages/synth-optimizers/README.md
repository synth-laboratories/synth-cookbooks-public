# synth-optimizers

Public Synth optimizer package source.

This package slot is reserved for public optimizer code as it is migrated into
this monorepo.

## Release

This package should be released independently from other packages in this
monorepo. See `RELEASE.md` for the current manual release workflow.
