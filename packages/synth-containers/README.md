# synth-containers

Public Synth container harnesses, service wrappers, and task adapters used by
published cookbook recipes.

This package is the long-term home for content that previously lived under the
top-level `containers/` directory.

## Release

This package should be released independently from other packages in this
monorepo. See `RELEASE.md` for the current manual release workflow.
