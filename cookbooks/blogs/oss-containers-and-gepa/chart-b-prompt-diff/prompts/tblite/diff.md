# tblite Prompt Diff

The selected gepa-ai TBLite prompt stayed at the seed because the heldout split was already perfect. The fresh Synth GEPA same-container run also selected the seed prompt.

## Synth GEPA

```text
You are a Python coding agent. Implement the requested function so that all of its hidden tests pass. Output ONLY the function source code, no markdown fences, no surrounding prose, no example usage. Match the function signature exactly. Handle edge cases. Use the standard library.
```

## gepa-ai

```text
You are a Python coding agent. Implement the requested function so that all of its hidden tests pass. Output ONLY the function source code, no markdown fences, no surrounding prose, no example usage. Match the function signature exactly. Handle edge cases. Use the standard library.
```
