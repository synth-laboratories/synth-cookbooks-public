[project]
name = "tblite-gepa-container"
version = "0.1.0"
description = "Public GEPA cookbook container for Terminal-Bench-Lite (real pytest verifier, OpenAI agent)."
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.110",
  "uvicorn>=0.27",
  "openai>=1.0",
  "pytest>=8.0",
]
